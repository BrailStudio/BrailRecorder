Empty on purpose — run `cargo tauri icon path/to/source-icon.png` to
generate the full icon set (32x32.png, 128x128.png, 128x128@2x.png,
icon.ico) from a single source image before running `cargo tauri build`.
A `tray.png` for the system tray icon needs to be added separately.
